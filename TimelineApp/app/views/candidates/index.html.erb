<h2>Candidates</h2>

<%= form_tag "/candidates/search", method: :get do %>
  <%= text_field_tag :search_term %>
  <%= submit_tag "Search" %>
<% end %>

<ul>
  <% @candidates.each do |candidate| %>
    <li>
       <%= link_to(candidate.name, candidate) %>
    </li>
  <% end %>
</ul>
